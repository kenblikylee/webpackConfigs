export default class MapAdapter {
  constructor() {
    this.name = 'MapAdapter'
  }
  marker() {
    console.warn(`${this.name}#marker 方法未实现!!!`)
  }
  polyline() {
    console.warn(`${this.name}#polyline 方法未实现!!!`)
  }
  polygon() {
    console.warn(`${this.name}#polygon 方法未实现!!!`)
  }
  circle() {
    console.warn(`${this.name}#circle 方法未实现!!!`)
  }
  text() {
    console.warn(`${this.name}#text 方法未实现!!!`)
  }
  layer() {
    console.warn(`${this.name}#layer 方法未实现!!!`)
  }
}